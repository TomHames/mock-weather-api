import React from 'react';
import ReactDOM from 'react-dom';

export default class WeatherData extends React.Component {
  render() {
    return (
      <div>
        <p>[Inserted Weather API Data]</p>
      </div>
    );
  }
}
